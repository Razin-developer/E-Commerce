{{> header}}

<div class="container mt-5">
  <!-- Welcome Section -->
  <div class="text-center mb-5">
    <a href="/master-delete-all-order" class="btn btn-danger btn-lg mt-4">Delete All Order</a>
  </div>
</div>

<!-- Responsive Table Section -->
<div class=" p-5">
  <div class="table-responsive">
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th scope="col"></th>
          <th scope="col">Admin</th>
          <th scope="col">Email</th>
          <th scope="col">Number of Orders</th>
          <th scope="col"></th>
          <th scope="col"></th>
        </tr>
      </thead>
      <tbody>
        {{#each details}}
        <tr>
          <th scope="row">&middot;</th>
          <td>{{this.name}}</td>
          <td>{{this.email}}</td>
          <td>{{this.no}}</td>
          <td>
            <a href="/master-view-orders/{{this._id}}" class="btn btn-primary">View Orders</a>
          </td>
          <td>
            <a href="/master-delete-orders/{{this._id}}" class="btn btn-danger">Delete</a>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>